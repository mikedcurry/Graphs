
def earliest_ancestor(ancestors, starting_node):
    lineage = {}
    stack = []
    stack = stack + [starting_node]

    while len(stack):
        line = stack.pop()
        print(line)
        # if not lineage.get(current)
        
        break

    return


